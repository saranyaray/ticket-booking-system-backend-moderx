# Database Configuration
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=ticketdb
PGPORT=5432

# Server Configuration
PORT=3000
NODE_ENV=development

# AI Model Configuration (optional)
AI_MODEL=claude-4-mini

# Feature Flags (optional - can also be set via DB)
# FEATURE_FLAGS_ENABLE_HAIKU_ROLLOUT=true
